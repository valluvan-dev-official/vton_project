# DCI-VTON SageMaker inference dependencies.
#
# torch / torchvision / torchaudio are provided by the CUDA base image
# (see Dockerfile) and are intentionally NOT pinned here to avoid
# clobbering the CUDA-matched build.

# ── DCI-VTON / Stable Diffusion stack (mirrors api/requirements-gpu.txt) ──
omegaconf==2.1.1
einops
transformers==4.46.3
albumentations
ftfy
regex
fvcore
iopath
open-clip-torch
kornia
torchmetrics
pytorch-lightning==1.4.2
git+https://github.com/openai/CLIP.git
# detectron2 is installed from the already-cloned source tree in the Dockerfile
# (pip install -e /opt/ml/repos/detectron2_repo). It must NOT be listed here
# because git+https://... would trigger a redundant second clone before compiling.
# No official prebuilt wheel exists for PyTorch 2.x (last release: v0.6, 2021).

# ── Image / array processing ──
numpy==1.26.4
pillow==10.3.0
opencv-python-headless==4.9.0.80
scikit-image==0.23.2

# ── SageMaker serving (TorchServe-based PyTorch inference toolkit) ──
sagemaker-inference
multi-model-server
requests-toolbelt
